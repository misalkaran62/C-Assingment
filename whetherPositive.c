#include <stdio.h>
int main()
{
    int num;

    printf("Enter the Number :");
    scanf("%d", &num);

    if (num > 0)
    {
        printf(" The Number is positve ");
    }
    else
    {
        printf("The Number is positive");
    }
    return 0;
}
